angular
    .module('Result')
    .controller('ResultController', ResultController);

function ResultController($scope, $filter, ResultService, ResultFactory, $cookies, toastr) {
	// $scope.hello = 111111;
    // console.log(111111);

}